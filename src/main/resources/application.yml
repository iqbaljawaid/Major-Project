server:
  port: 8081
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    password: admin
    url: jdbc:mysql://localhost:3306/major
    username: iqbal
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        enable_lazy_load_no_trans: allow
        event:
          merge:
            entity_copy_observer: allow
    show-sql: true
  mail:
    host: smtp.gmail.com
    password: bzqjuitwmfduuors
    port: 587
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
    username: iqbaljawaid.5822@gmail.com
 
